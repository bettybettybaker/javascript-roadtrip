// Challenge:

// Now using the specific array function that takes a piece of
// data off the back of an array, remove the last entry from
// your values array and store the result in a variable
// called bool.

///////////////////////// SOLUTION /////////////////////////

> var values = ["Jason", 4, true];
undefined

> var bool = values.pop()
