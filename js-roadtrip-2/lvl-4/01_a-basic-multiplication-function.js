// Challenge:

// In the uniqueMath.js file, build a function declaration called
// multiplyTrio that takes in three parameters. Inside the function,
// multiply the numbers together into one product, and return the result.
// You may use whatever parameter and variable names you’d like.


///////////////////////// SOLUTION /////////////////////////

function multiplyTrio(a, b, c){
return a*b*c;
}

multiplyTrio(1, 2, 3);
